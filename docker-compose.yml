version: '3.10'

services:
  db:
    image: postgres:latest
    hostname: '127.0.0.1'
    ports:
      - '5431:5432'
    environment:
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: '12345'
      POSTGRES_DB: 'db_swapi'